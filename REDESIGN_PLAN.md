# 📐 سند تحلیل و برنامه بازطراحی UI/UX
**سامانه مدیریت فعالیت‌های امنیتی — نسخه ۳ طراحی**
تهیه توسط تیم Product Design + UI/UX + Frontend Architecture

---

## ۱) اسکن معماری فعلی (Ground Truth)

| لایه | وضعیت |
|---|---|
| بک‌اند | Flask + Jinja + SQLite — **بدون تغییر** (طبق دستور) |
| Routeها | ۳۳ مسیر — **همه حفظ می‌شوند** |
| قالب‌ها | ۱۸ فایل HTML در `templates/` |
| استایل | `static/css/app.css` (۳۸۸ خط) + `print.css` |
| اسکریپت | `static/js/app.js` (تقویم شمسی) + `charts.js` (نمودار SVG) |
| آیکن | ۵۰ symbol دست‌ساز SVG در اسپرایت — بدون ایموجی ✅ |
| فونت | Vazirmatn چهار وزن، داخلی و آفلاین ✅ |
| تست | ۶۶ تست خودکار (selftest.py) سبز ✅ |

### نقاط قوت فعلی (حفظ می‌شود)
فلش‌پیام‌های واضح، فرم‌های پویای حوزه‌محور، تقویم شمسی پاپ‌آپ، نمودارهای SVG بدون وابستگی، خروجی Excel/PDF/CSV، سیستم نقش دوگانه، و گردش‌کار تسک.

---

## ۲) نقاط ضعف پیدا شده — هر مورد با دلیل

### 🔴 معماری فرانت‌اند (ریشه مشکل)
1. **۱۴۷ استایل inline در ۱۸ قالب** — `style="margin-bottom:14px"` و... در همه جا؛ یعنی قوانین طراحی در HTML پراکنده است، نه در Design System. نتیجه: هر تغییر ظاهری = دستکاری ده‌ها فایل → حس «پروژه دست‌ساز غیرمنسجم».
2. **نبود لایه ابزارک (Utility Layer)** — برای فاصله، فونت، رنگ کلاس استاندارد نداریم؛ هر صفحه خودش عدد می‌نویسد.
3. **تبدیل دیتا به ظاهر در قالب‌ها پراکنده است** — کلاس وضعیت (`st-done/st-doing`) با شرط سه‌تایی در ۵ قالب تکرار شده است.

### 🟠 تجربه کاربری
4. **«میانبر عملیات مهم» در داشبورد وجود ندارد** — در خواسته شماست؛ کاربر برای «ثبت فعالیت» باید ۲ کلیک برود.
5. **جدول فعالیت‌ها مرتب‌سازی ندارد** — ستون‌ها هدر ساده‌اند؛ مدیر نمی‌تواند بر اساس تاریخ/حوزه مرتب کند.
6. **انتخاب چندتایی وجود ندارد** — برای خروجی CSV از چند ردیف مشخص راهی نیست. (در خواسته شما: مرتب‌سازی، انتخاب چندتایی، CSV در صفحه فعالیت‌ها)
7. **انتخاب‌گر ستون‌های گزارش، ابرِ ۸۰ تایی چک‌باکس است** — بدون جستجو، گروه‌بندی یا پریست؛ در عمل قابل استفاده نیست.
8. **هدر مراحل ثبت فعالیت، صرفاً نوار متن است** — اندیکاتور واقعی ۲ مرحله‌ای (استپر) ندارد؛ حس Wizard نمی‌دهد.
9. **تعملای هدر نتیجه گزارش sticky نیست** و ابزارها هنگام اسکرول گم می‌شوند؛ فیلترها در موبایل/لپ‌تاپ ورپی زشت می‌شوند (فاقد حالت باز/بسته).
10. **صفحه ورود پنل راستش «تخت و خالی» است** — برای محصول سازمانی باید کارت شیشه‌ایِ بلند + سیگنال‌های اعتماد داشته باشد (نسخه، حالت اجرا، پیام امنیت).
11. **نوار بالایی وقت می‌میرد (dead space)** — سمت راست خالی؛ نه مسیر (Breadcrumb) نه اکشن سراسری.

### 🟡 بصری / هویت
12. **پالت «ایندیگو + خاکستری» امن ولی جنسریک است** — دقیقاً همان چیزی که خروجی‌های AI می‌سازند؛ برای حس طراحی اختصاصی به **امضای بصری** نیاز است (خط گرادیانی امضا روی تاپ‌بار، آیکن‌های دو‌رنگ اختصاصی، الگوی سطح).
13. **تایپوگرافی نمودارها ۱۰px است** — در پرینت مدیریتی کوچک و ناخوانا.
14. **اسکرول‌بار پیش‌فرض مرورگر** — جزئیات لوکس را می‌شکند.
15. **انیمیشن‌ها بدون احترام به `prefers-reduced-motion`** — جزئیت استاندارد سازمانی.
16. **حالت خالی صفحات فقط آیکن است** — بدون راهنمای گام بعدی (empty-state آموزشی).

---

## ۳) Design System v3 (زبان طراحی واحد)

### توکن‌ها
```
رنگ اصلی   ایندیگو #4F46E5  + امضای دوم: فیروزه #06B6D4 (مویرگ گرادیانی، چارت، فوکوس)
خنثی‌ها    slate سرد ۷ درجه (سطح→متن)   معنایی‌ها: ok/warn/bad/info + border متناظر
تایپو      122/125/13/135/15/20  وزن 400/500/700/800  اعداد: tabular-nums
فاصله      شبکه ۴px: ۴/۸/۱۲/۱۶/۲۰/۲۴/۳۲/۴۰   گوشه‌ها: ۸/۱۲/۱۶   سایه: ۳ لول (سبک)
```

### اجزا (همه از یک دستگاه)
- **دکمه**: primary/secondary/ghost/danger × سه سایز + دکمه آیکن-only ۳۲px
- **کارت**: هدرِ «تیتر + خط توضیح» + بدنه + فوتر اختیاری
- **جدول حرفه‌ای**: هدر مرتب‌ساز (کلاینتی، ▲▼)، چک‌باکس انتخاب، toolbar انتخاب (شمارنده + خروجی CSV مرورگر)، هدر sticky، hover نرم
- **فرم**: لیبل ۷۵۰/رینگ فوکوس ۴px/تاریخ سه‌بخشی + تقویم — بدون لمس منطق
- **flash**: رنگ‌بندی معنادی + آیکن — حفظ متن فعلی (تست‌ها به آن وابسته‌اند)
- **خالی**: آیکن + تیتر + راهنما + CTA
- **استپر ۲ مرحله‌ای** برای ثبت فعالیت، **آکاردئون جستجوی ستون** برای گزارش‌ها

### خروجی CSV انتخاب‌شده (مهم)
**بدون هیچ تغییر Route**: انتخاب ردیف‌ها → ساخت CSV در مرورگر با Blob (BOM فارسی) → دانلود. این یعنی درخواست «انتخاب چندتایی + CSV» بدون نقض «عدم تغییر بک‌اند» محقق می‌شود.

---

## ۴) برنامه صفحه‌به‌صفحه

| # | صفحه | تغییرات |
|---|---|---|
| ۱ | `base.html` | تاپ‌بار با مویرگ امضا + Breadcrumb از page_title؛ sidebar با badge تعداد (از context موجود)؛ utilities جدید؛ reduced-motion؛ اسکرول‌بار سفارشی |
| ۲ | `login` | کارت شیشه‌ای بلند سمت فرم، گلوی ملایم، meta پایین کارت؛ پنل برند: تایپو قوی‌تر + پالت امضا |
| ۳ | `dashboard` | **کارت میانبرها** (ثبت فعالیت/ورود Excel/گزارش/کاربران) + KPI همان؛ فونت چارت ۱۱px؛ ترتیب: KPI → میانبر+نمودار وضعیت → نمودارها → جدول‌های ۵آیتمی (فقط آمار، بدون لیست کامل) |
| ۴ | `activities` | فیلتر بار فشرده + دکمه نمایش/پنهان؛ **هدر مرتب‌ساز**؛ **چک‌باکس+toolbar انتخاب (CSV مرورگر)**؛ آیکن مشاهده در عملیات؛ density نرمال |
| ۵ | `activity_new` | **استپر ۱→۲ واقعی** + دکمه «مدیریت فیلدهای این حوزه» (فقط مدیر، کوچک ghost در همان ردیف) + سرصفحه بخش‌ها همین |
| ۶ | `activity_edit` | همان استایل‌های new؛ ستون پیوست کارت تمیزتر (ردیف فایل با آیکن MIME) |
| ۷ | `activity_view` | هدر فعالیت با تایل دامنه + اکشن‌ها گروهی؛ جدول فیلد/مقدار zebra نرم؛ ترد پاسخ‌ها حبابی‌تر؛ پیوست با آیکن MIME |
| ۸ | `reports` | فیلترها جمع‌وجور؛ **انتخاب‌گر ستون: آکاردئون + جستجو + پریست (همه/پایه/هیچ)**؛ toolbar نتیجه sticky |
| ۹ | `report_print` | هدر چاپ + فوتر صفحه «صفحه X از Y» (CSS counters)؛ فونت چارت بزرگ‌تر |
| ۱۰ | `import_excel` | دو کارت موجود ولی با dropzone بزرگ‌تر (drag&drop اختیاری، کلاینتی) |
| ۱۱ | `import_result` | KPI سه‌تایی موجود + جدول خطاها شیک‌تر + CTA |
| ۱۲ | `users/user_form` | ستون آواتار حروف اول؛ تگ نقش؛ فرم تک‌کارت |
| ۱۳ | `settings` | دو کارت موجود؛ «نکات» به alert-cards تبدیل شود |
| ۱۴ | `domains/fields` | اینپوت ویرایش inline تمیزتر؛ دستگیره مرتب‌سازی ▲▼ به آیکن |
| ۱۵ | `error` | صفحه حالت متمرکز با کد بزرگ ۸۰۰ |
| — | **همه** | حذف ۱۴۷ inline style → کلاس‌های utility (`mb-3`, `mt-2`, `ic-22`...) |

---

## ۵) تعهدات غیرقابل مذاکره
- ❌ بدون React/Vue/Angular/Next — فقط همین معماری Jinja + CSS/JS وانیلی
- ❌ بدون تغییر Route، منطق، دیتابیس، رفتار، نام‌فیلدها و پیام‌ها
- ✅ هر مرحله با selftest (۶۶ تست) + اسکرین‌شات همه صفحات + کنسولِ بدون خطا تأیید می‌شود
- ✅ خروجی: `secman.zip` با DB تازه + اسکرین‌شات‌های نهایی

## ۶) مراحل اجرا (بعد از تأیید)
1. `base.html` + توکن‌ها/utility ها  2. `login`  3. `dashboard`  4. `activities` (سنگین‌ترین)  5. فرم‌های ثبت/ویرایش/نمایش  6. `reports` + print  7. import/users/settings/domains/fields/error  8. پولیش نهایی + تست + زپ
